apiVersion: v1
kind: Service
metadata:
  name: {{ include "car-lookup-service.fullname" . }}
  labels:
    {{- include "car-lookup-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "car-lookup-service.selectorLabels" . | nindent 4 }}
